process ProcessOne = {
 service = MessageLogger {
    untracked vstring destinations = { "cout" }
          untracked PSet cout = { untracked string threshold = 
#"ERROR" 
# or switch to
"INFO" 
} 
}	



//Subproject specific modules
     source = EmptyIOVSource {
         string timetype = "runnumber"
         untracked uint32 firstRun = 1
         untracked uint32 lastRun = 1
         uint32 interval = 1
       }


//PopCon config
include "CondCore/DBCommon/data/CondDBCommon.cfi"
// eg to write the objects in sqlite

#replace CondDBCommon.connect = "sqlite_file:runinfo_pop_test.db"
#replace CondDBCommon.connect ="oracle://cms_orcon_prod/CMS_COND_21X_RUN_INFO"
replace CondDBCommon.connect ="oracle://cms_orcoff_prep/CMS_COND_21X_RUN_INFO"
#replace CondDBCommon.DBParameters.authenticationPath = "/nfshome0/xiezhen/conddb/"
replace CondDBCommon.DBParameters.authenticationPath = "."
// Database output service
    service = PoolDBOutputService{
	using CondDBCommon
        untracked string timetype="runnumber"
	VPSet toPut = { { string record = "RunInfoRcd" string tag = "runinfo_test" } }
	#untracked string logconnect = "oracle://cms_orcon_prod/CMS_COND_21X_POPCONLOG"
  untracked string logconnect = "sqlite_file:logruninfo_pop_test.db"
 }


//Common parameters to all subprojects
module Test1 = RunInfoPopConAnalyzer {
   string record = "RunInfoRcd"
   untracked bool loggingOn = True
   untracked bool IsDestDbCheckedInQueryLog = False
   bool SinceAppendMode = true
   PSet Source ={}
}

 path p = {Test1}
}


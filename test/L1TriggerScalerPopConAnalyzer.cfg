process ProcessOne = {
 service = MessageLogger {
    untracked vstring destinations = { "cout" }
          untracked PSet cout = { untracked string threshold = 
#"ERROR" 
# or switch to
"INFO" 
} 
}	



//Subproject specific modules
     source = EmptyIOVSource {
         string timetype = "runnumber"
         untracked uint32 firstRun = 1
         untracked uint32 lastRun = 1
         uint32 interval = 1
       }


//PopCon config
include "CondCore/DBCommon/data/CondDBCommon.cfi"
// eg to write the objects in sqlite

replace CondDBCommon.connect = "sqlite_file:l1trscaler_pop_test.db"

// Database output service
    service = PoolDBOutputService{
	using CondDBCommon
        untracked string timetype="runnumber"
	VPSet toPut = { { string record = "L1TriggerScalerRcd" string tag = "l1triggerscaler_test" } }
	untracked string logconnect = "sqlite_file:log.db"
   }


//Common parameters to all subprojects
module Test1 = L1TriggerScalerPopConAnalyzer {
   string record = "L1TriggerScalerRcd"
   untracked bool loggingOn = True
   untracked bool IsDestDbCheckedInQueryLog = False
   bool SinceAppendMode = true
   PSet Source ={}
}

 path p = {Test1}
}

